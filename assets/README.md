# MediKiosk assets

This folder holds images, icons, and styles used by the application.

The app keeps most visual assets inline (SVG data URIs, CSS animations in
`components/ui.py`, and DiceBear avatar URLs) so the project stays lightweight
and self-contained.